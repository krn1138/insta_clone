<h1>一覧</h1>
<table>
  <tr>
    <th>写真</th>
    <th>内容</th>
  </tr>
<% @posts.each do |post| %>
  <tr>
    <td><%= post.picture %></td>
    <td><%= post.content %></td>
    <td><%= link_to '詳細を確認する', root_path(@post) %></td>
  </tr>
<% end %>
</table>

<%= link_to '新しくブログを投稿する', new_post_path %>